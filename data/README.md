# Data files

- `curated_candidates.csv` is the manually controlled input.
- `pipeline.csv` is generated by merging curated rows with accepted ClinicalTrials.gov records.

Do not treat `pipeline.csv` as an authoritative database without reviewing its row-level sources and the latest automation report. Scheduled GitHub builds publish a freshly generated artifact but do not commit that output back to the repository.
